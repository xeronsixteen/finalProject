from django.urls import path

app_name = 'webapp'

urlpatterns = [
    # path('', IndexView.as_view(), name="index"),
    # path('product/add/', CreateProduct.as_view(), name="add"),
    # path('product/<int:pk>/update/', UpdateProduct.as_view(), name="update"),
    # path('product/<int:pk>/', ProductView.as_view(), name="product_view"),
    # path('product/<int:pk>/delete/', DeleteProduct.as_view(), name="delete"),
    # path('product/<int:pk>/review/add/', CreateReviewView.as_view(), name="product_review_create"),
    # path('review/<int:pk>/update/', UpdateReview.as_view(), name="update_review"),
    # path('review/<int:pk>/delete/', DeleteReview.as_view(), name="delete_review"),
    # path('review/<int:pk>/', OneReviewView.as_view(), name="one_review"),
    # path('reviews/', AllReviewView.as_view(), name="reviews"),

]
